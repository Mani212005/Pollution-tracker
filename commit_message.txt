Fix: Resolve Streamlit duplicate element ID errors

Add unique keys to various Streamlit components (plotly_chart, radio, slider, selectbox) across the application to prevent duplicate ID errors when rendering multiple instances of the same component, especially in the "Compare Cities" mode.